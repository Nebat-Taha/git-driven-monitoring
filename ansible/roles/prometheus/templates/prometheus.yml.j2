global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # This is the "Hook" for Workflow 3
  - job_name: 'dynamic_custom_exporters'
    file_sd_configs:
      - files:
          - '/etc/prometheus/file_sd/*.json'